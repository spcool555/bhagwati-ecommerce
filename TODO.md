# TODO

## Reviews section ("Open Up To A New Experience")
- [x] Add placeholder implementation in `main.js` for review slider + form (GET /api/public/allReviews, POST /api/public/addReview)
- [ ] Add HTML block to `index.html` near existing banner text with:
  - Review slider container (`#reviewsCarousel`)
  - Review form (`#addReviewForm`) with fields: name, stars, review
- [ ] Add CSS to `style.css` for layout and readability
- [ ] Quick manual test: open `index.html` and verify slider loads + form submits (endpoints can be mocked)

